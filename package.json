{
  "name": "storage-social", "version": "0.1.0", "private": true, "type": "module",
  "description": "Photo and video social publishing workspace for Storage AZ",
  "engines": { "node": ">=24.0.0" },
  "scripts": { "start": "node --env-file-if-exists=.env src/server.mjs", "dev": "node --env-file-if-exists=.env --watch src/server.mjs", "test": "node --test tests/*.test.mjs", "check": "node scripts/check.mjs", "backup": "node --env-file-if-exists=.env scripts/backup.mjs" }
}
